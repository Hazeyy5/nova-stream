import React from 'react'
import ReactDOM from 'react-dom/client'
import AudioPropsWindow from './components/AudioPropsWindow'
import './styles/global.css'

ReactDOM.createRoot(document.getElementById('root')!).render(
  <React.StrictMode>
    <AudioPropsWindow />
  </React.StrictMode>
)
